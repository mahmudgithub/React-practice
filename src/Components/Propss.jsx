import React from 'react'

export const Propss = (props) => {

    return (
        <div>
          <p>name: {props.name}</p>  
          <p>city: {props.city}</p>  
          <p>roll: {props.roll}</p>  
        
        </div>
    )
}
export default Propss;